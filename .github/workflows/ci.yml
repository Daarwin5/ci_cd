name: Shell Script CI

on:
  push:
    branches:
      - main
      - ci_cd
  pull_request:
    branches:
      - main

jobs:
  test-scripts:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Make shell scripts executable
        run: chmod +x ci_cd/function.sh ci_cd/while.sh

      - name: Run function.sh
        run: ./ci_cd/function.sh

      - name: Run while.sh
        run: ./ci_cd/while.sh

